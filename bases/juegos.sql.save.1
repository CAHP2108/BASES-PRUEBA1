DROP DATABASE IF EXISTS videojuegos
CREATE DATABASE videojuegos;

CREATE TABLE desarrolladora(
	nombre VARCHAR(20) PRIMARY KEY;
);

CREATE TABLE juego(
	id INT PRIMARY KERY,
	nombre VARCHAR(20),
	fk_desarrolladora
	FOREIGNKEY (fk_desarrolladora)
	REFERENCES desarrolladora(nombre)

);

CREATE TABLE personaje(
	nombre
);
INSERT INTO desarrollores VALUES ('Buggy Soft');
INSERT INTO
INSERT INTO personaje VALUES
('Cap. Salami', 10.0, 0),
('Cap. Cuchiller', 10.0, 0),
('Señor. Seítán', 10.0, 0),
('Señor. cucharilla ', 10.0, 0),
INSERT INTO
